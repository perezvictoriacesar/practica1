 //suma de naturales 
 let num,acomulador=0 ,formula;

 num=parseInt(promt ("ingresa la posicion n" ) );

 for(let index =1; index <= num;index++){ 
    acummulador+=index;

     }

     formula = (num*(num+1))/2;
     console.log("s1")+acumulador;
     console.log("+s2" +formula);
     if(formula===acumulador){ 
        console.log("son igules");
         } else console.log("No son iguales";)

          }